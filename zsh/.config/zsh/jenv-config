# jenv config
JENV_ROOT="$HOME/.jenv"
# Original Config
# This eval ... slows down zsh shell startup :(
#export PATH="$HOME/.jenv/bin:$PATH"
#eval "$(jenv init -)"

# Attemp 1 - --no-rehash and do it asynchronously -> NOT WORKING
#eval "$(jenv init - --no-rehash)"
#(jenv rehash &) 2> /dev/null

# Attemp 2 - Using mroth/evalcache plugin -> NOT Faster
#zsh_add_plugin "mroth/evalcache"
#_evalcache jenv init -

# Attemp 3 - Using shihyuho/zsh-jenv-lazy plugin -> WORKs :)
#plug "shihyuho/zsh-jenv-lazy"
